interface:
  display_name: "Pi Mono"
  short_description: "Repo-specific patterns and workflows for pi-mono"
  default_prompt: "Use the pi-mono repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true